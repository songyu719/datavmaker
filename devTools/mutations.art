import {dataElement} from "@/store/index";
import {useStore} from "@/store/index";
const store = useStore()
{{each mutations}}
export function  {{$value.name}}(payload:{{$value.interface}}) {
    store.commit("{{$value.name}}",payload)
}
{{/each}}